To compile the Question Bank, use the command:
    make compile

To run the Java Question Bank, use the command:
    make java_qb

To run the Python Question Bank, use the command:
    make python_qb

To run the C Question Bank, use the command:
    make C_qb